# Contributing

Contributions are welcome. Keep the runtime dependency-free unless a dependency has a clear cross-platform benefit that justifies the install and supply-chain cost.

Before opening a pull request:

```bash
npm test
npm run check
```

Cat artwork should be original or contributed with explicit redistribution permission. Add companion files under `plugins/touch-grass/assets/companions/<id>/` and follow the manifest described in that directory.
